
[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

# Keep Bun's install cache inside the project so it works under sandboxed
# environments that deny writes to ~/.bun.
[install]
cache = ".bun-cache"